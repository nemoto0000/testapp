<%= javascript_include_tag("picchange") %>
<%= stylesheet_link_tag 'picchange' %>
有料版にUPDATE!!するのがおすすめです！
<% form_tag :action => :index do %>
年齢:

<%= text_field_tag :from_age, params[:from_age] %>
~<%= text_field_tag :to_age, params[:to_age] %>
<%= submit_tag "検索" %>
<% end %>

<% if @targets.count > 0 #結果がなければ %>
<table>
<tr><td colspan="2">
<!-- relevant for the tutorial - start -->
<div id="pictures">
<% @targets.each do |target| %>
  <p type="hidden" class="uid"><%= target.uid %></p>
  <p type="hidden" class="prof"><%= target.name %>(<%= get_gender(target.sex) %>, <%= target.age %>)</p>
  <%= image_tag target.pic_big||='', :align=>'right', :size=>'300x300' %>
<% end %>
</div>
<font size="5" id='prof'><%= @targets[@targets.count-1].name %>(<%= get_gender(@targets[@targets.count-1].sex) %>, <%= @targets[@targets.count-1].age %>)</font>
</td></tr>
<tr><td>
<div class="grid_3 omega" id="na">
  <a href="#na">ダメ</a>
</div>
</td>
<td>
<div class="grid_3 omega" id="ok">
  <a href="#ok" onclick="#{remote_function(:url => {:controller => :likes, :action => :okey}, :with => 'ここに数字入れたい')};alert('o');return false;">OKEY!!!</a>
</div>
</td></tr>
</table>
<% end #結果がなければ %>

<!-- relevant for the tutorial - end -->
